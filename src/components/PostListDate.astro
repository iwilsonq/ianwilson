---
import { format } from "date-fns";
const { date } = Astro.props;

const formattedDate = format(date, 'do MMMM y')
---
<div class="flex text-gray-400 text-lg">
  <time datetime={date.toISOString()}>
    {formattedDate}
  </time>
  <div class="flex-grow border-t mt-4 ml-3" />
</div>
